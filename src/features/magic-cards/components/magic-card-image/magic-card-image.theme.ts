import { StyleSheet } from 'react-native';

export const P9MagicCardImageTheme = StyleSheet.create({
  containerStyle: {
    flexGrow: 1,
  },

  imageContainerStyle: {
    backgroundColor: '#ccc',
    borderRadius: 10,
  },

  imageStyle: {
    aspectRatio: 0.71794871794,
    flexGrow: 1,
    borderRadius: 10,
  },

  placeholderContainerStyle: {
    alignContent: 'center',
    justifyContent: 'center',
    position: 'absolute',
    bottom: 0,
    left: 0,
    right: 0,
    top: 0,
  },
});
